// Modifica da applicare al ToolpathGenerator.tsx

// Importa useCAMStore se non è già importato
import { useCAMStore } from '@/src/store/camStore';

// All'interno del componente ToolpathGenerator, trova e usa questi hook
const setStoreGcode = useCAMStore(state => state.setGcode);

// Modifica la funzione generateGCode per salvare il codice nel camStore
// e NON aggiungere l'editor custom

// Trova la porzione di generateGCode dove il G-code viene generato e modificala così:
const gcode = generateMillGCode(); // o qualsiasi generatore stai usando

// Dopo aver generato il codice, salvalo nello store e notifica il componente genitore
setStoreGcode(gcode);
onGCodeGenerated(gcode);

// Aggiungere questa sezione alla fine della funzione generateGCode
// per mostrare una notifica che spiega come visualizzare il codice generato
toast.success(
  "G-code generato con successo! Puoi visualizzarlo nella tab 'Editor G-Code'",
  { duration: 4000 }
);
